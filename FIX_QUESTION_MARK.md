# 🔧 Исправление знака "?" в Xcode

## Проблема

Галочка стоит в Target Membership, но знак "?" не пропадает.

---

## ✅ Решение 1: Пересоздать ссылки на файлы

### Шаг 1: Удалить файлы из проекта (НЕ удалять файлы!)

1. В Project Navigator выбери файлы с "?"
2. **Правой кнопкой** → **"Delete"**
3. В диалоге выбери **"Remove Reference"** (НЕ "Move to Trash"!)
   - Это удалит только ссылку из проекта, файлы останутся на диске

### Шаг 2: Добавить файлы заново

1. **Правой кнопкой** на папку `IG Planner` в Project Navigator
2. **"Add Files to 'IG Planner'..."**
3. Выбрать файлы:
   - `LocalizationManager.swift`
   - `LanguagePickerView.swift`
   - `AppleSearchAdsManager.swift`
   - `ApphudManager.swift`
   - `AdaptyManager.swift`
   - `AIGenerationAPI.swift`
   - `IGStoriesAPI.swift`
4. ✅ **ВАЖНО:** Галочка **"Copy items if needed"** — должна быть **ВЫКЛЮЧЕНА** (т.к. файлы уже в папке)
5. ✅ **"Add to targets:"** → Галочка на **"IG Planner"**
6. ✅ **"Create groups"** (не "Create folder references")
7. **Add**

---

## ✅ Решение 2: Исправить Location в File Inspector

Если файлы уже добавлены:

1. Выбери файл с "?" (например, `LocalizationManager.swift`)
2. File Inspector (правая панель)
3. В секции **"Identity and Type"**:
   - **Location:** Должно быть **"Relative to Group"**
   - Если там другой путь → нажми на иконку папки и выбери файл заново

---

## ✅ Решение 3: Игнорировать "?" (если проект компилируется)

**Важно:** Знак "?" может оставаться, но если:
- ✅ Галочка стоит в Target Membership
- ✅ Проект компилируется без ошибок
- ✅ Файлы используются в коде

То можно **игнорировать** знак "?". Это визуальная проблема Xcode, не влияющая на компиляцию.

---

## 🔍 Проверка

После исправления:

1. **Clean Build Folder**: `Cmd + Shift + K`
2. **Build**: `Cmd + B`
3. Если компилируется без ошибок → всё работает!

---

## ⚠️ Если "?" остаётся

Это может быть связано с:
- Git статусом (файлы не в git)
- Кэшем Xcode
- Проблемами с путями

**Решение:**
1. Закрыть Xcode
2. Удалить папку `DerivedData`:
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData
   ```
3. Открыть Xcode заново
4. Открыть проект
5. Clean Build Folder: `Cmd + Shift + K`
6. Build: `Cmd + B`

---

## 📝 Главное

**Если проект компилируется** → знак "?" можно игнорировать. Это не критично.
